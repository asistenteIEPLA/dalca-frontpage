/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Standard config optimized for Vercel
  // No static export, standard runtime
};

export default nextConfig;
